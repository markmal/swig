#######################################################################
# Makefile for Free Pascal test-suite
#######################################################################

LANGUAGE     = freepascal
SCRIPTSUFFIX = _runme.pp
FREEPASCALPATHSEPARATOR = "@FREEPASCALPATHSEPARATOR@"
FREEPASCALCYGPATH_W     = @FREEPASCALCYGPATH_W@
srcdir       = @srcdir@
top_srcdir   = @top_srcdir@
top_builddir = @top_builddir@

CPP_TEST_CASES = 

CUSTOM_TEST_CASES = 
#CUSTOM_TEST_CASES = intermediary_classname


include $(srcdir)/../common.mk

# Overridden variables here
SWIGOPT += -xmlout tree.xml -runtime_const $(SWIGOPTSPECIAL)

# Rules for the different types of tests
%.cpptest: 
	$(setup)
	+$(swig_and_compile_cpp)
	$(run_testcase)

%.ctest:
	$(setup)
	+$(swig_and_compile_c)
	$(run_testcase)

%.multicpptest: 
	$(setup)
	+$(swig_and_compile_multi_cpp)
	$(run_testcase)

# Rules for custom tests
#intermediary_classname.customtest: 
#	$(MAKE) intermediary_classname.cpptest SWIGOPTSPECIAL="

# Compiles Free Pascal files then runs the testcase. A testcase is only run if
# a file is found which has _runme.pas appended after the testcase name.
# Note FPC uses LD_LIBRARY_PATH under Unix, PATH under Cygwin/Windows and SHLIB_PATH on HPUX.
run_testcase = \
      $(MAKE) -f $(top_builddir)/$(EXAMPLES)/Makefile \
      FREEPASCALFLAGS='-Fu..' \
      FREEPASCALSRCS='*.pas' freepascal_compile

# Clean: remove testcase directories
%.clean:
	@if [ -d $* ]; then \
		rm -rf $*; \
	fi;

clean:
	@rm -f *.pas *.cxx *.o *.so *.exe *.exe.mdb *.dcu *.a *.ppu *.pp tree.xml
